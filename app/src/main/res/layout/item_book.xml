<?xml version="1.0" encoding="utf-8"?><!-- res/layout/item_book.xml -->
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <variable
            name="book"
            type="com.example.bookshelf.model.Book" />
    </data>

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="16dp">

        <TextView
            android:id="@+id/textViewTitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignParentStart="true"
            android:layout_alignParentLeft="true"
            android:layout_alignParentTop="true"
            android:text="@{book.title}"
            android:textSize="18sp" />

        <TextView
            android:id="@+id/textViewHits"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_below="@+id/textViewTitle"
            android:layout_alignStart="@+id/textViewTitle"
            android:layout_alignLeft="@+id/textViewTitle"
            android:layout_marginTop="8dp"
            android:text="@{String.format(@string/hits_format, book.hits)}"
            android:textSize="14sp" />

        <RadioButton
            android:id="@+id/radioButtonFavorite"
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:layout_alignParentEnd="true"
            android:layout_alignParentRight="true"
            android:layout_centerVertical="true"
            android:button="@drawable/ic_star_radio_button"
            android:checked="@{book.isFavorite}"
            android:contentDescription="Favorite" />
    </RelativeLayout>
</layout>
